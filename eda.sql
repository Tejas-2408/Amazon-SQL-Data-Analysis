-- EDA
SELECT
	*
FROM
	CATEGORY;

SELECT
	*
FROM
	CUSTOMERS;

SELECT
	*
FROM
	SELLERS;

SELECT
	*
FROM
	PRODUCTS;

SELECT
	*
FROM
	ORDERS;

SELECT
	*
FROM
	ORDER_ITEMS;

SELECT
	*
FROM
	PAYMENTS;

SELECT
	*
FROM
	SHIPPINGS;

SELECT
	*
FROM
	INVENTORY;

-- Number of orders retruned
SELECT
	COUNT(*)
FROM
	SHIPPINGS
WHERE
	RETURN_DATE IS NOT NULL;

-- Number of orders delivered successfully
SELECT
	COUNT(*)
FROM
	SHIPPINGS
WHERE
	RETURN_DATE IS NULL;